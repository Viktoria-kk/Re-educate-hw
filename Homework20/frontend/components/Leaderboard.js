"use client";
import { useUser } from "@/context/UserContext";
export default function Leaderboard({ rows }) { const { user } = useUser(); if (!rows.length) return <div className="state"><h2>The board is wide open</h2><p>Be the first player to score.</p></div>; return <div className="leader-table"><div className="leader-head"><span>Rank & player</span><span>Score</span><span>Correct</span><span>Answered</span></div>{rows.map((row, index) => <div key={row._id} className={`leader-row ${row._id === user?._id ? "current" : ""}`}><div><b className={`rank rank-${index + 1}`}>{index + 1}</b><strong>{row.username}</strong>{row._id === user?._id && <small>You</small>}</div><strong>{row.totalScore}</strong><span>{row.correctAnswers}</span><span>{row.answeredQuestions}</span></div>)}</div>; }
