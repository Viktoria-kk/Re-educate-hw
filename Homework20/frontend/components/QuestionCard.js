"use client";
export default function QuestionCard({ question, selected, result, onAnswer, busy }) { return <div className="question-card"><h2>{question.text}</h2><div className="answers">{question.options.map((option, index) => { let className = "answer"; if (selected === index) className += " selected"; if (result && index === result.correctAnswerIndex) className += " correct"; if (result && selected === index && !result.isCorrect) className += " incorrect"; return <button key={option} className={className} disabled={busy || Boolean(result)} onClick={() => onAnswer(index)}><span>{String.fromCharCode(65 + index)}</span>{option}</button>; })}</div>{result && <div className={`result ${result.isCorrect ? "success" : "miss"}`}><strong>{result.isCorrect ? "That’s right!" : "Not this time."}</strong><span>{result.isCorrect ? `+${result.earnedPoints} points added to your score.` : "The correct answer is highlighted above."}</span></div>}</div>; }
