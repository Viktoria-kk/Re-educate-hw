"use client";
import { useCallback, useEffect, useState } from "react"; import api, { errorMessage } from "@/lib/api"; import QuizCard from "@/components/QuizCard"; import Loading from "@/components/Loading"; import ErrorMessage from "@/components/ErrorMessage";
export default function QuizzesPage() { const [quizzes, setQuizzes] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const load = useCallback(async () => { setLoading(true); setError(""); try { setQuizzes((await api.get("/quizzes")).data); } catch (err) { setError(errorMessage(err)); } finally { setLoading(false); } }, []); useEffect(() => { load(); }, [load]); return <section><div className="page-heading"><span className="eyebrow">QUIZ LIBRARY</span><h1>Available quizzes</h1><p>Select a topic to begin. Each question can be answered once.</p></div>{loading ? <Loading label="Loading quizzes…"/> : error ? <ErrorMessage message={error} retry={load}/> : quizzes.length ? <div className="quiz-grid">{quizzes.map((quiz, index) => <QuizCard key={quiz._id} quiz={quiz} index={index}/>)}</div> : <div className="state"><h2>No quizzes available</h2><p>Run the backend seed command to add the quiz collection.</p></div>}</section>; }
