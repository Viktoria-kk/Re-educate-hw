"use client";
import { useEffect, useState } from "react"; import api, { errorMessage } from "@/lib/api"; import socket from "@/lib/socket"; import Leaderboard from "@/components/Leaderboard"; import OnlineUsers from "@/components/OnlineUsers"; import Loading from "@/components/Loading";
export default function LeaderboardPage() { const [rows, setRows] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { const update = (data) => { setRows(data); setLoading(false); }; api.get("/leaderboard").then(({ data }) => update(data)).catch((err) => { setError(errorMessage(err)); setLoading(false); }); if (!socket.connected) socket.connect(); socket.on("leaderboard:update", update); socket.emit("leaderboard:request"); return () => socket.off("leaderboard:update", update); }, []); return <section><div className="page-heading"><span className="eyebrow">LIVE RESULTS</span><h1>Leaderboard</h1><p>Rankings update automatically after every submitted answer.</p></div><div className="leader-layout"><div>{error && <p className="error">{error}</p>}{loading ? <Loading label="Loading leaderboard…"/> : <Leaderboard rows={rows}/>}</div><OnlineUsers/></div></section>; }
